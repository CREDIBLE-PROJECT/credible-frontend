import { colors, fonts } from './styleOptions';
import { deviceMediaQuery } from './mediaQuery';

const theme = {
  color: colors,
  font: fonts,
  device: deviceMediaQuery,
};

export default theme;
